import { atom } from "recoil";

const _ = atom({
  key: "@kokateam/router-vkminiapps/atoms/82kWuD",
  default: {
    views: [] as string[],
    panels: {},
    modals: [] as string[],
  },
});

export default _;
